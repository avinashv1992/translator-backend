#pragma once
#include <string>
#include "llm_client.h"
#if 0
class Translator {

    LLMClient llm;

public:
    std::string translate(
        const std::string& text,
        const std::string& lang
    );
};
#endif