#pragma once
#include <string>
#if 0
class LLMClient {

public:
    LLMClient();

    std::string sendPrompt(
        const std::string& prompt
    );

private:
    std::string apiKey;
    std::string apiUrl;
};
#endif