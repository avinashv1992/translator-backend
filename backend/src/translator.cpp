#include "translator.h"
#include "context.h"
#if 0
std::string Translator::translate(
    const std::string& text,
    const std::string& lang)
{
    //std::string prompt =
     // buildContext1(text,lang);

    return llm.sendPrompt(prompt);
}
#endif